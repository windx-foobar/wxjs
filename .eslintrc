{
  "extends": [
    "@nuxt/eslint-config"
  ],
  "overrides": [
    {
      "files": ["*.ts"],
      "rules": {
        "semi": ["error", "always"],
        "space-before-function-paren": ["error", "never"],
        "no-trailing-spaces": ["error"]
      }
    }
  ]
}
